# Dependencies (the engine is dependency-free; this is hygiene for contributors)
node_modules/
package-lock.json

# Build / cache
dist/
*.tsbuildinfo
.cache/

# Runtime (review-queue persistence; lives on a volume, never committed)
data/
*.tmp

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
