{
  "name": "@open-gates/mcp",
  "version": "0.0.1",
  "description": "Model Context Protocol server for Open Gates — drive the acceptance gate lifecycle as typed tools and resources.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=22.18" },
  "bin": { "open-gates-mcp": "server.ts" },
  "scripts": { "start": "node server.ts" },
  "comment": "The MCP SDK and zod are real dependencies and live ONLY in this subtree, so engine/src stays dependency-free and type-strippable. Run `npm install` here before `npm run mcp`.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
